# Public backend URL. Used by the Next.js chat proxy outside Docker.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional private backend URL for server-to-server traffic.
# API_INTERNAL_URL=http://backend:8000
